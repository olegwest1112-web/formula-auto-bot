import { sqliteTable, text, integer, index } from 'drizzle-orm/sqlite-core';
export const vehicles=sqliteTable('vehicles',{id:integer('id').primaryKey({autoIncrement:true}),payload:text('payload').notNull(),status:text('status').notNull(),published:integer('published').notNull().default(0),archived:integer('archived').notNull().default(0),version:integer('version').notNull().default(1),internalNote:text('internal_note').notNull().default(''),createdAt:text('created_at').notNull(),updatedAt:text('updated_at').notNull()},t=>[index('idx_vehicles_public').on(t.published,t.archived,t.status)]);
export const leads=sqliteTable('leads',{id:text('id').primaryKey(),payload:text('payload').notNull(),status:text('status').notNull().default('new'),notified:integer('notified').notNull().default(0),version:integer('version').notNull().default(1),createdAt:text('created_at').notNull(),updatedAt:text('updated_at').notNull()},t=>[index('idx_leads_status').on(t.status,t.createdAt)]);
export const media=sqliteTable('media',{id:text('id').primaryKey(),mime:text('mime').notNull(),size:integer('size').notNull(),createdAt:text('created_at').notNull()});
export const audit=sqliteTable('audit',{id:integer('id').primaryKey({autoIncrement:true}),actor:text('actor').notNull(),action:text('action').notNull(),entity:text('entity').notNull(),createdAt:text('created_at').notNull()});
export const settings=sqliteTable('settings',{id:integer('id').primaryKey(),payload:text('payload').notNull(),version:integer('version').notNull().default(1)});
export const rateLimits=sqliteTable('rate_limits',{id:text('id').primaryKey(),count:integer('count').notNull().default(1),expires:integer('expires').notNull()},t=>[index('idx_rate_expiry').on(t.expires)]);
